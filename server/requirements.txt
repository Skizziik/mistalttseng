# Зависимости локального XTTS-сервера.
# ВАЖНО: torch с поддержкой CUDA ставится отдельно (см. README / run.bat),
# потому что его нужно брать с правильного индекса под твою видеокарту.
coqui-tts>=0.24.0
flask>=3.0
numpy>=1.24
